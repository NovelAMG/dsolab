# LAB ONLY — deliberately pinned to stale releases so MDVM/Trivy report real,
# trackable SCA findings. Not for production. Restore with:
#   fastapi==0.116.1  uvicorn[standard]==0.35.0  openai==1.93.0
#   azure-identity==1.24.0  python-dotenv==1.1.1
#
# Transitive deps are pinned too — left unpinned they resolve to current at
# build time, silently patching away the findings we want.
fastapi==0.109.0
starlette==0.35.1
uvicorn[standard]==0.20.0
openai==1.30.1
azure-identity==1.16.0
python-dotenv==1.1.1
pydantic==2.5.0
requests==2.31.0
urllib3==1.26.17
cryptography==41.0.7
certifi==2023.7.22
jinja2==3.1.2
pyjwt==2.13.0
# httpx/httpcore pinned to a pair that still accepts h11 0.13; current httpcore
# requires h11>=0.16 and would fail resolution.
httpx==0.27.0
httpcore==1.0.5
h11==0.13.0
